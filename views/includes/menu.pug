.list-group
    a(href='/products' class=path==='/products'?"list-group-item active":"list-group-item").list-group-item Tümü
    if categories.length > 0
        each category in categories
            a(href=`/categories/${category.id}` class=category.id===selectedCategory?"list-group-item active":"list-group-item") #{category.name}